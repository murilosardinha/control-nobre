.card.card-body
  = form_for [@category] do |f|
    = render 'shared/errors_form', model: @category

    .row
      .form-group.col-lg-4
        = f.label :title, "Título"
        = f.text_field :title, class: "form-control"

    .row
      .form-group.col-lg-3.mt-2
        = f.submit "Salvar", class: "btn btn-success text-white" 